set(TARGET_LIBC_ENTRYPOINTS
  ${PORTFOLIO_LIBC_CTYPE}
  ${PORTFOLIO_LIBC_ERRNO}
  ${PORTFOLIO_LIBC_INTTYPES}
  ${PORTFOLIO_LIBC_POLL}
  ${PORTFOLIO_LIBC_REGEX}
  ${PORTFOLIO_LIBC_SEARCH}
  ${PORTFOLIO_LIBC_SETJMP}
  ${PORTFOLIO_LIBC_STDBIT}
  ${PORTFOLIO_LIBC_STDLIB}
  ${PORTFOLIO_LIBC_STRING}
  ${PORTFOLIO_LIBC_STRINGS}
  ${PORTFOLIO_LIBC_SYS_IOCTL}
  ${PORTFOLIO_LIBC_SYS_MMAN}
  ${PORTFOLIO_LIBC_SYS_PERSONALITY}
  ${PORTFOLIO_LIBC_SYS_PRCTL}
  ${PORTFOLIO_LIBC_SYS_TIME}
  ${PORTFOLIO_LIBC_WCTYPE}
)

set(TARGET_LIBM_ENTRYPOINTS
  ${PORTFOLIO_LIBM_COMPLEX}
  ${PORTFOLIO_LIBM_FENV}
  ${PORTFOLIO_LIBM_MATH}
)

set(TARGET_LLVMLIBC_ENTRYPOINTS
  ${TARGET_LIBC_ENTRYPOINTS}
  ${TARGET_LIBM_ENTRYPOINTS}
)
