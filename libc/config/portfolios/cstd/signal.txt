# Portfolios for signal (PORTFOLIO_LIBC_SIGNAL)

set(PORTFOLIO_LIBC_SIGNAL)

if((LIBC_TARGET_OS_IS_LINUX AND (LIBC_TARGET_ARCHITECTURE_IS_AARCH64 OR LIBC_TARGET_ARCHITECTURE_IS_X86_64)) AND (LLVM_LIBC_FULL_BUILD))
  list(APPEND PORTFOLIO_LIBC_SIGNAL
      libc.src.signal.pthread_sigmask
  )
endif()

if(LLVM_LIBC_FULL_BUILD)
  list(APPEND PORTFOLIO_LIBC_SIGNAL
      libc.src.signal.kill
      libc.src.signal.raise
      libc.src.signal.sigaction
      libc.src.signal.sigaddset
      libc.src.signal.sigaltstack
      libc.src.signal.sigdelset
      libc.src.signal.sigemptyset
      libc.src.signal.sigfillset
      libc.src.signal.signal
      libc.src.signal.sigprocmask
  )
endif()
